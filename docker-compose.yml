services:

  postgres:

    image: postgres:17

    container_name: mini-mes-postgres

    environment:
      POSTGRES_DB: mesdb
      POSTGRES_USER: mesuser
      POSTGRES_PASSWORD: secret

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: